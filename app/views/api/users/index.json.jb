@users.map do |user|
  {
    id: user.id,
    name: user.name,
    username: user.username,
    number_of_lists: user.lists.length,
    name_of_lists: user.lists.map do |list|
      {
        id: list.id,
        name: list.name,
      }
    end,
  }
end
